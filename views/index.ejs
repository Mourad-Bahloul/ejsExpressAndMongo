<h2>Recently added</h2>
<div>
  <% books.forEach((book) => { %>
  <a href="/books/<%= book.id %>">
    <img
      src="<%= book.coverImagePath %>"
      alt="<%= book.title %>"
      height="150px"
      width="100px"
    />
  </a>
  <% }) %>
</div>
